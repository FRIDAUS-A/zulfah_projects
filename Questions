ssignment 
1. Write an SQL query to display the result of an arithmetic expression.
10 + 15 - 5 * 2 should display 15
2. Write a SQL query to display three numbers in three columns.
3. Write a query to display a string "This is my MySQL journey, I Love 
Assignments ".
4. Write a SQL statement that displays all the information about all 
salespeople in the salesman table above
5. Write a SQL statement to display specific columns such as names and 
commissions for all salespeople in the salesman table above
6. In the table above. Write a query to display the columns in a specific 
order, such as order date, salesman ID, order number, and purchase 
amount for all orders
7. In the table above as well, write a SQL query to identify the unique 
salespeople ID. Return salesman_id.
8. From the table below, write a SQL query to locate salespeople who live in 
the city of 'Paris'. Return salesperson's name, city.
9. From the table below, write a MySQL query to find customers whose 
grade is 200. Return customer_id, cust_name, city, grade, salesman_id.
10. From the table below, write a MySQL query to find the Nobel Prize 
winner(s) for the year 2021. Return Date, subject and winner.
11. From the table above, write a MySQL query to find the Nobel Prize winner 
in ‘Literature’ for 1971. Return winner
12. From the table above, write a MySQL query to locate the Nobel Prize 
winner ‘Dennis Gabor'. Return year, subject.
13. From the table above, write a MySQL query to find the Nobel Prize 
winners in the field of ‘Physics’ since 2033. Return winner.
14. From the table above, write a MySQL query to find the Nobel Prize 
winners in ‘Chemistry’ between the years 1965 and 1975. Begin and end 
values are included. Return year, subject, winner, and country.
15. From the table above, write a MySQL query to retrieve the details of the 
winners whose first names match with the string ‘Louis’. Return year, 
subject, winner, country, and category.
16. From the table above, write a MySQL query that combines the winners 
in Physics, 1970 and in Economics, 1971. Return year, subject, winner, 
country, and category.
17. From the table above, write a MySQL query to find Nobel Prize winners 
for the subject that does not begin with the letter 'P'. Return year, subject, 
    winner, country, and category. Order the result by year, descending.
    18. In the table structure above, write a MySQL query to insert a record with 
    your own value into the table countries against each column.
    19. In the table structure above, write a MySQL query to insert one row into 
    the table countries against the column country_id and country_name.
    20. In the table structure above, write a MySQL query to create duplicate of 
    countries table named country_new with all structure and data.
    21. In the table structure above, write a MySQL query to insert NULL values 
    against region_id column for a row of countries table.
    22. Write a MySQL query to insert 3 rows by a single insert statement.
    23. Write a MySQL query insert rows from country_new table to countries 
    table.
    24. In the table above, write a query to display the name (first_name, 
    last_name) and salary for all employees whose salary is not in the range 
    $10,000 through $15,000.
    25. Write a query to display the name (first_name, last_name) and 
    department ID of all employees in departments 60 or 100 in ascending 
    order.
    26. Write a query to display the name (first_name, last_name) and salary for 
    all employees whose salary is not in the range $10,000 through $15,000 
    and are in department 30 or 100.
    27. Write a query to display the name (first_name, last_name) and hire date 
    for all employees who were hired in 1987.
        28. Write a query to display the first_name of all employees who have both 
        "b" and "c" in their first name.
        29. Write a query to display the last name, job, and salary for all employees 
        whose job is that of a Programmer or a Shipping Clerk, and whose salary 
        is not equal to $4,500, $10,000, or $15,000.
        30. Write a query to display the last name of employees whose names have 
        exactly 6 characters.
        31. Write a query to display the last name of employees having 'e' as the 
        third character.
        32. Write a query to display the jobs/designations available in the employees 
        table.
